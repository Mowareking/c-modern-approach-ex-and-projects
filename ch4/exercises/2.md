### Solution

In C89, the division of (-8)/5 could return -1 or -2 as it is implementation-defined behaviour but -(8/5) would always give -1. 

In C99, it always truncates towards 0 so they would be equal.